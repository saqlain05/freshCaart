# Migration `20201107063959-profile`

This migration has been generated by ykn88 at 11/7/2020, 12:09:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Profile" (
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "firstName" TEXT NOT NULL,
    "lastName" TEXT NOT NULL,
    "shopName" TEXT NOT NULL,
    "phone" TEXT NOT NULL,
    "whatsapp" TEXT NOT NULL,
    "address" TEXT NOT NULL,
    "city" TEXT NOT NULL,
    "pincode" TEXT NOT NULL,
    "openTime" TEXT NOT NULL,
    "closeTime" TEXT NOT NULL,
    "maxOrderAcceptTime" TEXT NOT NULL,
    "longitude" TEXT,
    "latitude" TEXT,
    "userId" INTEGER NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("userId")
);
INSERT INTO "new_Profile" ("createdAt", "updatedAt", "firstName", "lastName", "shopName", "phone", "whatsapp", "address", "city", "pincode", "openTime", "closeTime", "maxOrderAcceptTime", "longitude", "latitude", "userId") SELECT "createdAt", "updatedAt", "firstName", "lastName", "shopName", "phone", "whatsapp", "address", "city", "pincode", "openTime", "closeTime", "maxOrderAcceptTime", "longitude", "latitude", "userId" FROM "Profile";
DROP TABLE "Profile";
ALTER TABLE "new_Profile" RENAME TO "Profile";
CREATE UNIQUE INDEX "Profile_userId_unique" ON "Profile"("userId");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201106165329-change3..20201107063959-profile
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -42,9 +42,8 @@
   privateData        String?
 }
 model Profile {
-  id              Int      @default(autoincrement()) @id
   createdAt       DateTime @default(now())
   updatedAt       DateTime @updatedAt
   firstName       String   
   lastName        String   
@@ -58,10 +57,11 @@
   closeTime       String   
   maxOrderAcceptTime String   
   longitude       String?   
   latitude        String?   
-  user            User?   @relation(fields: [userId], references: [id]) 
-  userId          Int?    
+  user            User    @relation(fields: [userId], references: [id]) 
+  userId          Int 
+  @@id([userId])   
 }
 model Category {
   id        Int       @default(autoincrement()) @id
```


