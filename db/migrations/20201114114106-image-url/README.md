# Migration `20201114114106-image-url`

This migration has been generated by saqlain05 at 11/14/2020, 5:11:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Product" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "name" TEXT NOT NULL,
    "price" REAL NOT NULL,
    "imageUrl" TEXT,
    "description" TEXT NOT NULL,
    "minQuantity" INTEGER NOT NULL,
    "stock" BOOLEAN NOT NULL,
    "measureUnit" TEXT NOT NULL,
    "categoryId" INTEGER NOT NULL,

    FOREIGN KEY ("categoryId") REFERENCES "Category"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Product" ("id", "createdAt", "updatedAt", "name", "price", "imageUrl", "description", "minQuantity", "stock", "measureUnit", "categoryId") SELECT "id", "createdAt", "updatedAt", "name", "price", "imageUrl", "description", "minQuantity", "stock", "measureUnit", "categoryId" FROM "Product";
DROP TABLE "Product";
ALTER TABLE "new_Product" RENAME TO "Product";
CREATE TABLE "new_Profile" (
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "firstName" TEXT,
    "lastName" TEXT,
    "shopName" TEXT,
    "phone" TEXT,
    "whatsapp" TEXT,
    "address" TEXT,
    "city" TEXT,
    "pincode" TEXT,
    "openTime" TEXT,
    "closeTime" TEXT,
    "maxOrderAcceptTime" TEXT,
    "longitude" TEXT,
    "latitude" TEXT,
    "userId" INTEGER NOT NULL,
    "imageA" TEXT,
    "imageB" TEXT,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("userId")
);
INSERT INTO "new_Profile" ("createdAt", "updatedAt", "firstName", "lastName", "shopName", "phone", "whatsapp", "address", "city", "pincode", "openTime", "closeTime", "maxOrderAcceptTime", "longitude", "latitude", "userId", "imageA", "imageB") SELECT "createdAt", "updatedAt", "firstName", "lastName", "shopName", "phone", "whatsapp", "address", "city", "pincode", "openTime", "closeTime", "maxOrderAcceptTime", "longitude", "latitude", "userId", "imageA", "imageB" FROM "Profile";
DROP TABLE "Profile";
ALTER TABLE "new_Profile" RENAME TO "Profile";
CREATE UNIQUE INDEX "Profile_userId_unique" ON "Profile"("userId");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201114090047-reset..20201114114106-image-url
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -78,9 +78,9 @@
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
   name        String   
   price       Float    
-  imageUrl    String   
+  imageUrl    String?  
   description String   
   minQuantity Int      
   stock       Boolean  
   measureUnit String   
```


